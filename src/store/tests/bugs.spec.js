import { addBug } from "../bugs";

describe("bugsSlice", () => {
  describe("action creators", () => {
    it("addBug", () => {
      const result = addBug({ description: "a" });
      const expected = {};
    });
  });
});
